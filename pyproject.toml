[tool.poetry]
name = "python-project-lvl2"
version = "0.1.0"
description = ""
authors = ["Malafeev Artem"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
flake8 = "^3.8.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
